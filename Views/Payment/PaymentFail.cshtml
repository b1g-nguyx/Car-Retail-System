@{
	ViewData["Title"] = "Payment Fail";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .fail-container {
            max-width: 600px;
            margin: 100px auto;
            text-align: center;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        }
        .fail-icon {
            font-size: 60px;
            color: #D19C97;
        }
        .btn-custom {
            background-color: #D19C97;
            border-color: #D19C97;
        }
        .btn-custom:hover {
            background-color: #c2867f;
            border-color: #c2867f;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="fail-container">
            <i class="bi bi-x-circle-fill fail-icon"></i>
            <h2 class="mt-3" style="color: #D19C97;">Thanh toán thất bại!</h2>
            <p>Đã xảy ra lỗi trong quá trình thanh toán. Vui lòng thử lại hoặc liên hệ hỗ trợ.</p>
            <a href="/" class="btn btn-custom">Quay về trang chủ</a>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>